# Xuper network observation

This branch runs the original, unmodified Xuper APK in an Android emulator and records only non-sensitive network metadata: DNS hostnames, TLS SNI hostnames, HTTP Host headers, destination IPs, and UID-filtered socket destinations. No proxy, certificate injection, pinning bypass, payload decryption, credentials, tokens, or packet payloads are published.
